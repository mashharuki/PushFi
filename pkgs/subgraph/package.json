{
  "name": "subgraph",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ pushfi",
    "create-local": "graph create --node http://localhost:8020/ pushfi",
    "remove-local": "graph remove --node http://localhost:8020/ pushfi",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pushfi",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.44.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}