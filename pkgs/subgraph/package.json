{
  "name": "subgraph",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ pushfi2",
    "create-local": "graph create --node http://localhost:8020/ pushfi2",
    "remove-local": "graph remove --node http://localhost:8020/ pushfi2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pushfi2",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
